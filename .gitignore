node_modules/
dist/
.build/
*.log
.DS_Store
.omc/
.openswarm/
# openswarm run artifacts (tests and smoke scripts emit these at repo root)
results.jsonl
dead-letter.jsonl
# swarmkit-eval run output + content-addressed store
.eval-runs/
.runs/
# packed local-harness tarballs for sandbox evals
eval/.artifacts/
legacy/eval/.artifacts/
# npm pack artifacts (anywhere in the tree)
*.tgz
# parity harness run output (docs/67); evidence is uploaded, not committed
artifacts/parity/
.claude/
.omx/
.sessionlog/
# internal audit work products (kept local, not shipped)
openswarm-audit.md
openswarm-remediation-plan.md
# local reference checkouts (cloned repos, not tracked as submodules)
references/
# compiled platform binaries (build artifacts)
packages/*/openswarm
packages/*/openswarm.exe
packages/*/libopentui.dylib
packages/*/libopentui.so
legacy/packages/cli-*/libopentui.*
legacy/packages/cli-*/openswarm
# claude-agent-sdk native helper, co-located by build-binary.ts (~205MB)
packages/*/claude
packages/*/claude.exe

# Downloaded Tree-sitter grammar assets (doc 49, run 'npm run grammars:update')
assets/grammars/
packages/cli/dist/
legacy/.eval-runs/
