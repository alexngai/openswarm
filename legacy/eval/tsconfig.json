{
  "// note": "Standalone tsconfig for experiment-running code. NOT part of the shipped build (tsconfig.build.json only includes src/**). Experiments run via `bun eval/...` so no emit.",
  "compilerOptions": {
    "target": "ES2022",
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    "strict": true,
    "noEmit": true,
    "skipLibCheck": true,
    "esModuleInterop": true,
    "resolveJsonModule": true,
    "types": ["node"]
  },
  "include": ["**/*.ts"]
}
