{
  "name": "openswarm-cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "openswarm CLI for the dsh-based stack: eval-facing `topology cascade` matching the legacy contract (docs/01 Phase 5)",
  "main": "src/index.ts",
  "bin": { "openswarm": "bin/openswarm.mjs" },
  "dependencies": {
    "openswarm-swarm": "*",
    "openswarm-llm-openai": "*"
  }
}
